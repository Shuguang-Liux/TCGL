spring:
  application:
    name: provider
  cloud:
    nacos:
#      server-addr: @nacosHost@
      config:
#        namespace: @namespace@
        file-extension: @fileType@
        server-addr: @nacosHost@
        group: @group@
      discovery:
        server-addr: @nacosHost@=
        group: @group@
#  profiles:
#    active: @active@
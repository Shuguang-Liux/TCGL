spring:
  application:
    name: provider
  cloud:
    nacos:
#      server-addr: @nacosHost@
      config:
#        namespace: @namespace@
        file-extension: @fileType@
        server-addr: @nacosHost@
        group: @group@
      discovery:
        server-addr: @nacosHost@
        group: @group@
        heart-beat-interval: 10
#  profiles:
logging:
  level:
    org.springframework: debug
#    active: @active@